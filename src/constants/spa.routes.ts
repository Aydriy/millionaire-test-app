const ROOTS_QUIZ = 'quiz';
const ROOTS_RESULT = 'result';

export const PATH_QUIZ = {
  ROOT: `/millionaire-test-app/${ROOTS_QUIZ}`,
};

export const PATH_RESULT = {
  ROOT: `/millionaire-test-app/${ROOTS_RESULT}`,
};
